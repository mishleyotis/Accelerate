| id | sev | disposition | finding | what was done |
|---|---|---|---|---|
| AUD-0001 | BLOCKER | DESIGNED_OUT | The recording substrate is JSON, not the workbook: zero research steps write a sheet, and the workbook is exported once at the end | engine/workbook.py is the only writer and every step appends to it; a reopened workbook carries search, evidence, synthesis, gates and coverage. |
| AUD-0002 | BLOCKER | DESIGNED_OUT | research_handoff.json is the de-facto inter-skill contract, displacing the workbook the owner designates | handoff.py emits a PROJECTION that names the workbook as authority in its own _contract block; Handoff_Lock carries the hash the assessment compares. |
| AUD-0003 | BLOCKER | DESIGNED_OUT | Of the three canonical output artifacts, one has no producer, one ships in a format the app cannot classify, and only the workbook has both a producer and a reader | reports.py produces both .docx artefacts; the e2e test classifies all three with the app's own classifier. |
| AUD-0004 | BLOCKER | FIXED | In-process subagents receive no SessionStart brief, and the live Routine dispatches every producer that way | SubagentStart hook carries the brief to in-process producers through additionalContexts. |
| AUD-0005 | BLOCKER | FIXED | The routing chain's second hop is dead: 50 of 55 surface-map.md rows point at a path that does not exist | 49 anchors repaired; audit_skills ceiling 8 -> 0. |
| AUD-0006 | BLOCKER | DESIGNED_OUT | Resume after a mid-category interrupt skips the interrupted subcap and reports the state clean | orient serves the volleyed subcap and may not report clean while any work is open. |
| AUD-0007 | BLOCKER | DESIGNED_OUT | orient.py's entire gate view is permanently blind: floors_gate output has three readers and no writer | floors_gate.run WRITES $RUN/07_qa/floors_<cat>.json and Gate_Log; an unrun gate reads NOT_RUN, never PASS. |
| AUD-0008 | BLOCKER | DESIGNED_OUT | `ledger.py stats` raises NameError on every invocation, so R27's token-budget checkpoint rule cannot run | ledger.stats returns a count and a checkpoint decision. |
| AUD-0009 | BLOCKER | DESIGNED_OUT | The orient.py --skeleton template passes validation unmodified: form-filling produces a closed subcap | quality.is_boilerplate / is_fluent_but_empty refuse the skeleton at the WRITE; the subcap stays open. |
| AUD-0010 | BLOCKER | DESIGNED_OUT | $RUN is container-local with no persistence and no scheduled driver; the documented resume needs a human | runstate.resume reads entity, position and catalogue drift from the workbook; the two anchors cannot ship as tokens. |
| AUD-0011 | BLOCKER | DESIGNED_OUT | Both scripts the pinned workbook mandates — strip_working_area.py and patch_validator.py — do not exist in any tree | strip_working_area.py and patch_validator.py now exist; the strip refuses while the three analysis fields have no surviving copy. |
| AUD-0012 | BLOCKER | MITIGATED | The pinned workbook v2.0.0 fails required-sheet rule 1 against its own validator | the contract's required sheet set IS its generated set, so the self-contradiction cannot recur in the artefact this repo produces. |
| AUD-0013 | BLOCKER | DESIGNED_OUT | No production code path anywhere runs a workbook validator | validator.validate is called by the gate, the handoff builder and the CLI; a workbook cannot reach the handoff without passing it. |
| AUD-0014 | BLOCKER | FIXED | The app cannot distinguish 'not in scope' from 'in scope and unscored' — 44 of 49 in-scope rows are labelled out-of-scope | in_scope_unscored is its own bucket; the stage is reported once. |
| AUD-0015 | BLOCKER | DESIGNED_OUT | orient.py — the mandated session opener — issues work cards containing 15 literal {entity} placeholders | the card binds the entity and is refused if a token survives; a query with an unbound token is refused at the ledger. |
| AUD-0016 | BLOCKER | DESIGNED_OUT | R32's "STUB_ values fail the gates" is false — an all-STUB synthesis passes floors_gate --require-synthesis | quality.is_boilerplate / is_fluent_but_empty refuse the skeleton at the WRITE; the subcap stays open. |
| AUD-0017 | BLOCKER | DESIGNED_OUT | The facet-coverage honesty check is not implemented; its only proxy is disarmed by a waiver the same agent writes | every DQ facet must be answered or declared NOT_RUN with a reason the gate reads; a bare waiver is refused. |
| AUD-0018 | BLOCKER | OPEN | The repo solved reviewer independence by construction in one agent and explicitly inverted it in the research challenge | not addressed in this pass. |
| AUD-0019 | BLOCKER | DESIGNED_OUT | The STUB skeleton passes the gate it was built to fail | quality.is_boilerplate / is_fluent_but_empty refuse the skeleton at the WRITE; the subcap stays open. |
| AUD-0020 | BLOCKER | DESIGNED_OUT | Aspiration launders staleness: a future-dated 'planned' fact makes 2019 evidence CURRENT | recency_band bands a future date UNVERIFIED and ages against the run's pinned reference date, not today. |
| AUD-0021 | BLOCKER | DESIGNED_OUT | Proxy-only evidence closes as FACT and publishes as M4 with HIGH confidence | quality.claim_label_supported refuses FACT resting on proxy searching. |
| AUD-0022 | BLOCKER | DESIGNED_OUT | The >=20-items-per-category floor is computed, reported, and then not used | the >=20-item category floor is a blocking gate term. |
| AUD-0023 | BLOCKER | DESIGNED_OUT | Presence != Utilization has no code: five handoff fields are hardcoded empty and schema-blessed | the four facets are read from the workbook; one that never ran is NOT_RUN with a reason, never []. |
| AUD-0024 | BLOCKER | OPEN | The research challenge has no structural independence — and the plugin's own claimed exemplar does not either | not addressed in this pass. |
| AUD-0025 | BLOCKER | DESIGNED_OUT | No gate anywhere requires a challenge verdict to exist | a missing or non-vocabulary Challenge_Verdict is a blocking finding. |
| AUD-0026 | BLOCKER | DESIGNED_OUT | Fluent emptiness clears every R32 detector: gate output byte-identical to the golden fixture | quality.is_boilerplate / is_fluent_but_empty refuse the skeleton at the WRITE; the subcap stays open. |
| AUD-0027 | BLOCKER | OPEN | The artefact-retention half of invariant 4 was never built: `import_files.gcs_uri` has zero writers and zero readers | not addressed in this pass. |
| AUD-0028 | BLOCKER | FIXED | `register_evidence` hardcodes origin='producer', so the `internal` evidence origin can never be set | origin is an argument and validated; internal keeps its claim type and is labelled. |
| AUD-0029 | BLOCKER | FIXED | Internal-sourced evidence is silently demoted from FACT to INFERENCE instead of being refused or verified | origin is an argument and validated; internal keeps its claim type and is labelled. |
| AUD-0030 | BLOCKER | FIXED | The intake failure record is written durably and read by nothing: `parser_observations` has no reader and no grant outside the worker | get_run_progress returns intake_observations grouped worst-first, and migration 0057 carries the grant that call is owed. |
| AUD-0031 | BLOCKER | OPEN | qa_auditor.py cannot tell a good workbook from a garbage one, and it is what writes the governance issue register | not addressed in this pass. |
| AUD-0032 | BLOCKER | FIXED | A real production research_handoff.json satisfies 3 of the 12 fields the current research skill's own schema requires, and nothing in dma-assessment reads it | dma-assessment Phase 0 keys on the WORKBOOK and Handoff_Lock's catalogue hash, with a HARD STOP on drift; the JSON is documented as an index. |
| AUD-0033 | BLOCKER | DESIGNED_OUT | Citations in a delivered client report do not resolve, and the check that would catch it is documented but not implemented | one unresolvable citation refuses the whole report render. |
| AUD-0034 | BLOCKER | OPEN | A producer already fabricated an enrichment it never ran, and the defect class has 6 of 6 findings open and 0 closed | not addressed in this pass. |
| AUD-0035 | BLOCKER | OPEN | The production Routine is still missing two of three connector families, unchanged since the finding was raised | not addressed in this pass. |
| AUD-0036 | BLOCKER | DESIGNED_OUT | ledger.py stats — the only budget-check tool R27 names — crashes with NameError on every invocation and reports no search count | ledger.stats returns a count and a checkpoint decision. |
| AUD-0037 | BLOCKER | DESIGNED_OUT | The >=40 search-op wall has zero mechanical enforcement — orient.py reports 45/40, says "state clean", hands over the next card and exits 0 | at the ceiling orient leads do_first with STOP and withholds the card. |
| AUD-0038 | BLOCKER | OPEN | The scope-mode conversation budgets contradict the >=40 search-op cap by 1.5x-3.5x, in the skill's own numbers | not addressed in this pass. |
| AUD-0039 | BLOCKER | FIXED | report_parser.py is bound to a superseded 12-section report; every v8 section from §3 on is stored under the wrong kind | the HEADING decides the section kind; the number fallback is gone, and an unrecognised numbered section keeps its own name with an observation. |
| AUD-0040 | BLOCKER | OPEN | Nothing in the repo or the skills resolves to the three pinned templates; the only renderers target two other, mutually incompatible structures | not addressed in this pass. |
| AUD-0041 | BLOCKER | OPEN | Offering linkage is asserted, never resolved — and the referential store that would resolve it exists in the database with zero readers | not addressed in this pass. |
| AUD-0042 | BLOCKER | FIXED | The category-grain peer store has no feeder: the pinned workbook removed Peer_Benchmarks and the missing-tab path records nothing | the contract carries Peer_Benchmarks under the app's own column spellings, and the missing-tab path records the absence. |
| AUD-0043 | BLOCKER | FIXED | Handoff_Lock — the peer-set immutability mechanism both templates depend on — exists nowhere in the repo or the workbook | Handoff_Lock exists and lock_peer_set freezes the cohort once, refusing a different one. |
| AUD-0044 | BLOCKER | MITIGATED | The pinned Assessment Report names 17 workbook input sheets and 0 of them exist in the pinned workbook | report_spec names each section's workbook inputs and a section whose every input is empty refuses rather than rendering. |
| AUD-0045 | BLOCKER | FIXED | AG-01 accepts a self-REJECTED recommendation, so the template's one hard rule is enforced by nothing | AG-01 READS the verdict; a self-REJECTED item is refused and an off-vocabulary verdict is named. |
| AUD-0046 | BLOCKER | FIXED | 'Stale metric in the impact table' is unenforceable: dma_impact[].current is invisible to the CG-07 grain lock | `current` is in CG-07's score keys, so dma_impact[].current is compared to what the run serves; `target` deliberately is not. |
| AUD-0047 | BLOCKER | FIXED | Invariant 1's gate is a literal-import regex; three evasions to a live inference endpoint pass it | all three evasions fail; the sanctioned local embedding model passes. |
| AUD-0048 | BLOCKER | FIXED | Invariant 7 is violated, and the unsanctioned colour resolver is the one that wins — 50 call sites to 1 | one authored resolver, one generated runtime resolver, gate N, and BD-04 out of the textless backlog. |
| AUD-0049 | BLOCKER | FIXED | The production colour resolver paints a grey swatch for a null score, violating invariants 6 and 9 | one authored resolver, one generated runtime resolver, gate N, and BD-04 out of the textless backlog. |
| AUD-0050 | BLOCKER | FIXED | The acceptance ledger claims CI enforces invariant 7; nothing does, and the module it names is not the one that ships | one authored resolver, one generated runtime resolver, gate N, and BD-04 out of the textless backlog. |
| AUD-0051 | BLOCKER | FIXED | The 836 subcap count is hardcoded into coverage maths: a complete v7.0 run reports 101.8% coverage and negative gaps | coverage denominators come from the engagement set; >100% raises, an unknown denominator returns null; validate_contracts counts the catalogue's categories. |
| AUD-0152 | BLOCKER | DESIGNED_OUT | ERS is a contract column with a full calculator and nothing joining them — empty in every run ever produced | engine/ers.py computes it at the write and RECOMPUTES across the register (corroboration is a property of the register). Specificity reads the RRF consensus and BM25 fit where 02_search carries them and SAYS SO when it falls back; corroboration counts source IDENTITIES, not rows, matching single_source_fact. |
| AUD-0153 | BLOCKER | DESIGNED_OUT | All sixteen report sections have no owner: the renderer refuses a missing section and nothing writes one | engine/narrative.py makes a section a RECORD (Weighing, Absence_Basis, Assumptions, Bias_Notes, Inference_Tags, computed Accuracy_Basis) with an independent Review_Verdict; report-research-producer, report-assessment-producer and report-validator generated from the spec by gen_report_agents.py --check so a new section fails the build until it has an owner. The renderer now refuses an unreviewed section. |
| AUD-0155 | BLOCKER | MITIGATED | Nothing in the engine knew what a run cost; the $5/pillar ceiling was a hope | engine/cost.py prices a run from the rate card and states four levers with their arithmetic: capability grain (x0.20), turn discipline (x0.26), context hygiene (x0.50), model tiering (x0.65, a PLACEHOLDER and labelled as one) -> $12.46 against $20. MITIGATED not DESIGNED_OUT: three levers are arithmetic, the fourth is unmeasured, and none is enforced by the harness yet. |
| AUD-0158 | BLOCKER | FIXED | The technographic scan named no provider, so a register built from web hits could not be reconciled against the app's declared sources | contract v4 adds Tech_Register.Providers (required, a list) with TECH_PROVIDERS and TECH_BROKERS. A row whose only providers are brokers may not wear CONFIRMED — two brokers reselling one crawl is one observation. `techscan status` prints by_provider, broker_share and providers_never_run, which is the owner's requirement as a number rather than an intention. |
| AUD-0159 | BLOCKER | FIXED | Explorium was unreachable from the research tier and three agent files still taught the retracted 'no live API' story | Explorium has THREE doors and only one needs a key: the Vibe Prospecting MCP connector (session-authenticated, already in the auto-approve list, measured at 392/357/147 technologies across three clients), the ingest scan (no Secret Manager key, genuinely dark), and an owner-supplied export that `engine.techscan import-explorium` parses in the five shapes the app's own package parser has met. The correction check is now a scan of the whole plugin rather than a hand-kept tuple. |
| AUD-0160 | BLOCKER | MITIGATED | The T3 drilldown opened onto three empty states: its fields were contract-optional, unenforced, and never captured upstream | The research run now CAPTURES what the drawer renders: Tech_Register.DMA_Impact at the served contract's own 40-90 word band, a Tech_Peer_Deployments sheet at (TS_ID, Peer) grain, peer_coverage COMPUTED from it, and a second Clay pass over the peers frozen in Peer_Benchmarks. What remains open and is the OWNER'S call: for the customer audience `items[*].dma_impact` is stripped unconditionally by redaction.py's default-deny, so that card is blank by policy however well it is written — the rule names the submit-time gate that would replace it as the real fix, and building that gate is a decision about a measured leak (sell copy on 51 of 51 rows of one client), not a defect to route around. |
| AUD-0161 | BLOCKER | FIXED | The eight-card floor was unreachable through the only sanctioned writer, and one report's section N overwrote the other's | A list section (insight_card, finding, recommendation) now requires --card and appends per card; the update is keyed on the composite (Report, Section_ID, Card_ID) through the new RunWorkbook.update_row_where. `narrative.state` measures a list section across its cards and against its card floor. A test writes all sixteen sections through the real writer and renders both reports — a path no test had ever exercised. |
| AUD-0163 | BLOCKER | FIXED | Peer_Benchmarks was written at peer grain and the app's parser discards every row that is not at category grain | The peer SET stays frozen in Handoff_Lock, where a set belongs; the tab is now the per-category grid, one row per in-scope category. The app's parser learned the engine's paired Peer_Names/Peer_Scores lists (a fixed-width contract cannot grow a column per peer) and refuses to zip lists of different lengths rather than mis-attribute a score. A test walks a peer set from prelim.peers through the real parser. |
| AUD-0164 | BLOCKER | FIXED | Two completeness verdicts were true by construction and a forged declaration was quieter than a missing one | A pillar sheet is measured on rows that carry RESEARCH (a dominant claim, or evidence that is not NO_EVIDENCE), so a freshly created run reads SHORT rather than POPULATED. Report_Narrative reports its section coverage instead of a bare row count, and defers the blocking to narrative.require_ready rather than duplicating it. A NEVER_EMPTY sheet named in empty_sheet_reasons is ILLEGAL_DECLARATION and blocks — writing around a refusal should be louder than tripping it, not quieter. Gate_Log joins NEVER_EMPTY: two assessment sections read it as their only input. |
| AUD-0165 | BLOCKER | FIXED | Entity_Timeline had a writer, a gate and no reader anywhere in the shipped system | Carried, not dropped: 01_evidence/entity_timeline.json is a machine extra, and the tab now speaks the served surface's TWO vocabularies — a three-token direction (Signal) and an eight-token class (Kind) — plus the body, maturity effect and claim label C1 renders. The nine old event classes are BRIDGED rather than refused, because a run pinned to an earlier engine wrote those words and its events still have to filter. A PRELIM declaration now declares the tab it owns with the same ladder, at the stricter of the two floors, and prelim.require_complete — which described itself as the gate orient calls and was called by nothing — gates package assembly. |
| AUD-0168 | BLOCKER | FIXED | A register row could promote with no ts_id, render as a live button, and route to a page reporting it does not exist | Fixed at the SUBMIT gate, not the column. `_check_minted_row_ids` refuses a row of any of the five minted-id lists (ts_id, ic_id, f_id, fa_id, wn_id) that carries no id, and bypasses on a declared empty_state. Putting it on the column instead — ALTER COLUMN ts_id SET NOT NULL — would re-arm exactly the trap migration 0050 removed: a section that legitimately promotes empty writes one envelope-only row with every item column NULL, and the insert would abort the whole promote with a driver error naming a column instead of a verdict naming a path. 0050 says it in as many words: 'The gate is the enforcement; the NOT NULL was belt-and-braces that only ever bit the one row the gate does not describe.' The unique index does not help either — Postgres treats NULLs as distinct without NULLS NOT DISTINCT, so a null row is unroutable, not unwritable. Render side: an unroutable row is now a non-interactive card with a title saying why, because to a reader a click that dead-ends and a drilldown that was never built are the same thing. |
| AUD-0169 | BLOCKER | FIXED | The Client Research Profile is written, validated, rendered — and structurally uningestable by the app | RAISED, NOT FIXED — it is a distinct piece of app-side ingest work and naming it is more useful than a half-done version. All eight sections of one of the two reports reach no table in the app, while page packs name 'Client Profile DOCX' as the source of truth for firmographics, the leadership roster, focus-area verbatim quotes and the financial series — data the producer then fetches out of Drive by hand because the app holds none of it. The recipe: give the profile its own artefact slot in `_classify_artefact` (matching `client[_ ]profile.*\.docx$`, as classification.py already does), download it beside the report in job_main, parse it with report_key='client_research', extend _TITLE_KINDS for its six unmapped headings, and namespace the stored kind by report so an assessment `evidence_sources` row and a profile one stay distinguishable in document_sections. CLOSED by AUD-0171 on 2026-08-30: the decoy is gone, the profile is classified, downloaded, parsed and namespaced `client_research:`, and test_profile_reaches_the_app.py walks the REAL rendered document from the engine's renderer to the rows the app would store. Left OPEN beside its own fix for half a day, which is the same shape of silence it was filed about. |
| AUD-0170 | BLOCKER | FIXED | A second run for the same client silently merged into the first run's folder, locally and on Drive | open_folder reported created:false and overwrote run_manifest.json with the second run's identity; package copied the second run's deliverables in beside the first's and overwrote all three fixed-name machine extras; and because deliverable filenames carry the reference date, a second run on a different date left TWO scoring workbooks in one folder — from which the app's scan keeps exactly one artefact per kind, chosen by rank then iteration order. The folder became a mix of two runs with an arbitrary winner. Now the previous package moves whole into `_superseded/<run_id>/` with a SUPERSEDED.json naming what replaced it, the current one stays at the root where every reader looks, and the folder keeps its name because runs.source_folder_id keys on it. This mirrors the versioning the system already does server-side (one active run per entity, predecessor demoted to SUPERSEDED and RETAINED) rather than inventing a second scheme. The archive is excluded from the app's scan by path segment, so retention cannot create the ambiguity it exists to remove. |
| AUD-0171 | BLOCKER | FIXED | The Client Research Profile is now ingestable: classified, downloaded, parsed, namespaced | Closes AUD-0169. The profile has its own artefact slot matching the pattern classification.py was ALREADY using for it (client_profile, priority 3) — classified, recorded, then dropped, the AUD-0091 shape. It is downloaded beside the report, parsed, and its section kinds are namespaced `client_research:` because both reports produce evidence_sources and both produce a findings-shaped section. Measured over the real rendered document: 51 rows across 8 sections, six of which resolved to `unmapped:*` until the profile's own six patterns were added LAST in the table, after every assessment-report pattern, so nothing there could move. A ReportSection now names its own artefact_id, because persist_package attributed every section to one report_artefact_id — right while one report was ingested, a provenance hole the moment two were. |
| AUD-0172 | BLOCKER | FIXED | There was no stage key anywhere, so a sheet could not be required at one stage and meaningless at the other | Contract v5 records `stage` (research \| assessment) and SHEET_STAGE names the sheets that belong to one. An out-of-stage sheet reads OUT_OF_STAGE and does not block; a research workbook carrying assessment rows reads AHEAD_OF_STAGE and DOES block, because that is a figure the stage says cannot have been struck. `stage_of` falls back to the app's own inference so an upgraded workbook does not read as an assessment with three empty tabs. |
| AUD-0173 | BLOCKER | FIXED | The three tabs the app reads and the engine never wrote now exist at the assessment stage | Closes AUD-0166, which was filed NOT_THIS_ENGINE and which the owner has now decided belongs to the assessment stage. Columns are the app's own header spellings, the same technique PEER_BENCHMARK_COLUMNS documents. `engine.grains recompute` states both grains and `engine.grains recommendations` projects the assessment report's §7 rows into the tab — projected, never authored, because the recommendations already exist as Report_Narrative cards. Both refuse at the research stage. The assessment report's §3 now reads the STATED grains rather than only the subcap sheets, because H4's grain lock forbids re-deriving a pillar figure by averaging subcaps. Tests round-trip both through the app's real parsers. |
| AUD-0176 | BLOCKER | FIXED | Reading the new stage key through RunWorkbook broke the only route from research to a strip | Introduced by AUD-0173's own change and caught by the end-to-end walk, which is why that walk exists. `validate` opens the file with openpyxl ON PURPOSE — rules 1 and 2 judge a file that may not BE a valid run workbook, so RunWorkbook's accessors cannot be assumed. `wb.metadata()` therefore raised AttributeError inside `handoff.build`, and `strip` refuses without a handoff: a run could finish every category and never hand off. Fixed with one `_metadata(wb)` reader on the raw workbook, which also absorbs the copy `_rule7_run_id` was carrying — the third copy was how the fourth got written wrong. Two regression tests: the stage is read off the FILE when no caller states it, and validate takes a path and never a RunWorkbook. A SECOND half was found in the same module: `--expect-scores` was `action="store_true"`, so ABSENT meant False, and False is not 'no opinion' — it is the caller asserting the research stage. Passed straight through, the command line hard-coded research semantics for every workbook it was ever pointed at, which is the same defect the stage key exists to remove, one layer up. Now BooleanOptionalAction with default None: omit to read the recorded stage, pass --no-expect-scores to assert research. |
| AUD-0179 | BLOCKER | FIXED | 70 of the 86 MCP tools a session attaches prompted on every call, and not one of them had ever been ruled on | Owner, 2026-08-30: 'check that all MCP connector tools are usually autoapproved for all runs; I do not constantly have to approve tool calls.' Measured by feeding the REAL hook the 86 tools one session carried: 16 approved, 70 prompting. The hook had only ever learned the ENRICHMENT connectors, because those are what stopped a scheduled firing; every other attached server had never been looked at. The existing guard could not have caught it — it scans the PLUGIN's markdown, and the plugin names none of those servers. SERVER_SURFACES now splits each stable-segment server into `read` (auto-approved) and `withheld` (refused ON THE RECORD, listed rather than omitted, because an absent tool cannot be told apart from one nobody thought about); WITHHELD_SUFFIXES does the same for the opaque-UUID connectors. audit_autoapprove.py runs the real hook per tool — never a re-implementation, which would agree with itself by construction — and reports ALLOWED / WITHHELD / GUARDED / UNCLASSIFIED. Now 124 of 184 approved, 58 refused, 2 guarded, 0 unclassified. A token-based lint refuses a write smuggled into a read set, and has a test proving it fires. |
| AUD-0180 | BLOCKER | FIXED | The settings grant was a server wildcard, so it silently overruled every write the hook refuses | Two mechanisms answer one question and the broader one wins without anybody being told. `mcp__Google_Drive__*` had been granting `trash_file` and `share_file` — both refused by the hook — for as long as the wildcard existed. The list is read out of the plugin TREE, and a doc is part of the tree, so naming `mcp__Slack__slack_send_message` once in CLIENT-SELECTION.md §3.5 was about to grant every Slack tool including sends, in the same change that deliberately withheld that one. The grant is now derived from the hook's own table: a classified server is granted by EXACT read tool name, an unclassified one keeps the wildcard because no finer record of it exists, and the connector keeps its prefix wildcard. Every classified server is granted whether or not the tree names it, so the settings half survives a session whose hooks bound from a stale install. Two tests: no classified server is wildcarded, and every granted tool is one the hook would also approve. |
| REQ-0001 | BLOCKER | DESIGNED_OUT | The client folder is created at the END, so a run that stops early leaves nothing an operator can find | engine.assemble open_folder creates '<Entity> - DMA' at `start` with run_manifest.json at status IN_PROGRESS, records it in Run_Metadata.client_folder, and pushes it to intake; `package` COMPLETES that folder rather than creating one. A run without one is the watchdog state NO_CLIENT_FOLDER. |
| REQ-0002 | BLOCKER | DESIGNED_OUT | The binding rests on free text nobody checked: no financial statement was read, no revenue line examined, and AskUserQuestion was never used | engine/preflight.py: the basis is a preflight-v1 DOCUMENT carrying the financial-statement review (revenue lines, each naming the LOB it implies), the LOB census (ACCEPT/REJECT per sub-vertical, every material LOB examined) and the AskUserQuestion exchange with the owner's verbatim answer. The binding must MATCH the answer; two material LOBs make the question mandatory. sv_basis/mode_basis/lob_census are RENDERED from the file, so they cannot drift from it. |
| REQ-0003 | BLOCKER | DESIGNED_OUT | No preliminary research: the run profiles the capabilities and never the institution, so the Client Research Profile has nothing to render from | engine/prelim.py: six sections (financials, firmographics, leadership, timeline, peers, tech_baseline), each closed by RESEARCH or by a DECLARED absence with its ladder. `orient` serves no category card while any is open, and every narrative section must cite registered evidence. |
| REQ-0004 | BLOCKER | DESIGNED_OUT | The workbook validates while six of nineteen tabs are empty — the validator checks shape, and nothing checked content | engine/completeness.py: every tab is populated or carries a recorded reason in Run_Metadata.empty_sheet_reasons; nine sheets may never be declared empty. Blocks handoff and package. REF_Method now has a writer, rendered from the contract. |
| REQ-0005 | BLOCKER | DESIGNED_OUT | Agents stop mid-run with no self-healing: the watchdog cannot see a new DMA and cannot restart one | engine/registry.py: append-only run registry written at every start and round-tripped through Drive, so a fresh container sees the population. watchdog.sweep reads it (MISSING_LOCALLY carries the recovery command); every row carries a resume plan naming the agent and prompt; --revive dispatches through agent_run.py. HALTED and UNREADABLE are never revived automatically. |
| AUD-0052 | MAJOR | DESIGNED_OUT | The reports are rendered from the JSON plane, never curated from the workbook — which is the mechanism behind the unresolved citations | both reports are curated from the workbook's sheets at render time. |
| AUD-0053 | MAJOR | FIXED | 1-gates.md, the designated 'reading a verdict' file, is missing 37 of the connector's 69 gate ids — including CG-30 | 1-gates.md carries a generated census of all 69 registry ids; --check fails a stale build. |
| AUD-0054 | MAJOR | FIXED | Nothing reprints the brief after compaction, resume or fork, and compaction has no destination file at all | the brief prints on all five SessionStart sources and compaction has a destination in routing.md. |
| AUD-0055 | MAJOR | FIXED | The dispatch preamble re-asserts connectors and output format but never routing | the headless preamble carries routing, memory, the submit boundary and explain_gate. |
| AUD-0056 | MAJOR | OPEN | The real entry document of an unattended run is a ~40,000-character Routine prompt stored outside the repo, not the 377-byte brief | not addressed in this pass. |
| AUD-0057 | MAJOR | FIXED | Hooks bind once at session start, so a self-provisioning or stale-plugin firing can never have run the brief | SubagentStart hook carries the brief to in-process producers through additionalContexts. |
| AUD-0058 | MAJOR | FIXED | Two of five traced tasks are unreachable from the brief: reviewer-rejected card (3+ hops) and resume-after-compaction (no destination) | all six mintable id classes route; the compaction path has a section. |
| AUD-0059 | MAJOR | MITIGATED | CHAIN INTEGRITY computes no verdicts, and two of its seven checks read sheets that do not exist | Coverage verdicts are computed values recomputed on every write, never template tokens. |
| AUD-0060 | MAJOR | MITIGATED | The catalogue-hash lock the Client Profile template asserts is built nowhere | the catalogue hash exists, is written into Handoff_Lock and is compared by verify_handoff_lock and validator rule 7. |
| AUD-0061 | MAJOR | DESIGNED_OUT | The v4.2 validator rejects the pinned template workbook, and the migration script the template prescribes does not exist | one contract object is both the required and the generated shape; patch_validator migrates a pre-v3 workbook and refuses to drop a column. |
| AUD-0062 | MAJOR | FIXED | The counts rule the Client Profile states is enforced by nothing, and the live skills still carry the wrong literals | 66 stale literals corrected, the retired category's blocks removed, and check_taxonomy_drift keeps it so. |
| AUD-0063 | MAJOR | DESIGNED_OUT | No stuck-state detection exists for a research run; the watchdog that exists watches only synthesis | watchdog.py reports STALLED / HALTED / GATE_FAILED / UNGATED / AT_BUDGET_CEILING for a research run. |
| AUD-0064 | MAJOR | DESIGNED_OUT | Validator rule 2 does not catch an added column, and rule 5 is vacuous on a blank Evidence_IDs cell | rule 2 compares the whole header (an added column fires); rule 5 has no blank branch; rule 3 compares the id SET. |
| AUD-0065 | MAJOR | DESIGNED_OUT | research_handoff.json does not carry triangulation, why_it_matters or dma_impact — the strip does delete analysis the validator will not notice | the handoff carries triangulation, why_it_matters and dma_impact, and the strip refuses until it does. |
| AUD-0066 | MAJOR | DESIGNED_OUT | Two disjoint workbook production paths; nothing generates the pinned template's shape | one contract object is both the required and the generated shape; patch_validator migrates a pre-v3 workbook and refuses to drop a column. |
| AUD-0067 | MAJOR | FIXED | Chain join 2 is silently broken at the app boundary: SubCap_IDs is never read into evidence rows | the ledger read uses the alias table that already knew SubCap_IDs. |
| AUD-0068 | MAJOR | MITIGATED | Client Profile §8.1 routes 13 tabs to two workbooks; 13 of 13 are absent from the one workbook that exists | the tabs the profile routes to are either in the contract or reported as absent by contract.compare_to_workbook. |
| AUD-0069 | MAJOR | MITIGATED | None of the three pinned template Drive ids resolves anywhere in the repo or the archive | no code path can resolve the three Drive ids; contract.py --workbook reports divergence when one is exported. |
| AUD-0070 | MAJOR | FIXED | Installed skill drift: diagnostic_questions.md claims ~836 questions and holds 71, on the retired 17-category taxonomy including the killed P1C5 | 66 stale literals corrected, the retired category's blocks removed, and check_taxonomy_drift keeps it so. |
| AUD-0071 | MAJOR | FIXED | Installed skill drift: capability_criteria.md holds 85 = 17 x 5 on the retired category count, and its fifth level is M5 | 66 stale literals corrected, the retired category's blocks removed, and check_taxonomy_drift keeps it so. |
| AUD-0072 | MAJOR | FIXED | Production candidate selection dedupes by entity, contradicting the connector's own tested request-grain rule | the entity grain stays (it is the measured-correct one) and an absorbed DIFFERENT request is now named, with what it was absorbed into. |
| AUD-0073 | MAJOR | DESIGNED_OUT | The contradicts-probe detector cannot recognise the shipped contradicts query, and nothing tells the agent how to tag it | probes_contradicts reads the QUERY's adversarial operators; a label with no query does not pass. |
| AUD-0074 | MAJOR | OPEN | Query fidelity: two of the five MECE facets have no pre-built query at all, and no query can reach the temporal arc the works DQ demands | not addressed in this pass. |
| AUD-0075 | MAJOR | OPEN | map-fact cannot distinguish a capability fact from an unrelated sentence, and never abstains | not addressed in this pass. |
| AUD-0076 | MAJOR | DESIGNED_OUT | The R22 anti-clone guard as specified does not exist; the build-time namesake measures queries and cannot block | quality.evidence_smear computes sibling overlap from the workbook and blocks the gate. |
| AUD-0077 | MAJOR | FIXED | A sub-vertical overlay and its base siblings both enter the engagement set; the binder validates neither --sv nor --scope | Taxonomy.selected refuses an unknown scope or sub-vertical, an overlay supersedes its base, and a workbook refuses another SV's variants. |
| AUD-0078 | MAJOR | DESIGNED_OUT | Zero-evidence subcaps are handed off as ceiling_band 'M2', and category ceilings become float scores | an unresearched subcap carries a null band; category ceilings stay band words and never become floats. |
| AUD-0079 | MAJOR | DESIGNED_OUT | Absence detection is a 6-alternative regex with 29% recall and a 100% false-positive rate on the probe set | absence detection: 19/19 recall and 0/7 false positives on the battery that scored the old regex 4/14 and 3/3. |
| AUD-0080 | MAJOR | DESIGNED_OUT | Negative-finding ladders are self-attested; a 2-rung ladder is published as a '4-rung ladder' | ladder_report counts rungs and names a rung whose query was never fired. |
| AUD-0081 | MAJOR | OPEN | The R28 fishing gate keys on a field the pipeline does not set, and accepts a one-character reason | not addressed in this pass. |
| AUD-0082 | MAJOR | DESIGNED_OUT | The challenge layer is never required to exist, and a FAILED provisional challenge still reports HIGH confidence | a missing or non-vocabulary Challenge_Verdict is a blocking finding. |
| AUD-0083 | MAJOR | DESIGNED_OUT | Cited evidence ids are never resolved — the archive's own golden fixture cites a non-existent item | every cited id is resolved against the register; an id outside the run is refused at the write. |
| AUD-0084 | MAJOR | DESIGNED_OUT | The no-evidence default fabricates M2 — 45 of 49 subcaps in the golden run | an unresearched subcap carries a null band; category ceilings stay band words and never become floats. |
| AUD-0085 | MAJOR | DESIGNED_OUT | orient.py reports 'state clean' over interrupted subcaps — the re-hydration command's one imperative list is wrong | orient serves the volleyed subcap and may not report clean while any work is open. |
| AUD-0086 | MAJOR | OPEN | R23's checksum halt is uninvokable as documented, and nothing surfaces a stalled research run | the checksum halt is now a real comparison (verify_handoff_lock), but R23's documented command surface was not reconstructed. |
| AUD-0087 | MAJOR | OPEN | The research skill's durable record is unreadable by the app it feeds | not addressed in this pass. |
| AUD-0088 | MAJOR | OPEN | open_conflict renders under 'HIGH confidence' because the report filter keys on the wrong field | not addressed in this pass. |
| AUD-0089 | MAJOR | FIXED | `run_seq` is allocated by an unguarded read-modify-write with no unique constraint | migration 0056 adds the partial unique index; the entity row is locked before the maximum is read. |
| AUD-0090 | MAJOR | OPEN | The live overview page renders an evidence-mode badge from a field the API never sends | not addressed in this pass. |
| AUD-0091 | MAJOR | FIXED | `01_evidence/` — the richest evidence store in every package — is classified and then dropped by the ingest | the package evidence index is classified, parsed and merged asymmetrically. |
| AUD-0092 | MAJOR | OPEN | The internal-vs-public contradiction record exists in the corpus and has no destination in the schema | not addressed in this pass. |
| AUD-0093 | MAJOR | FIXED | secrets.md understates the one credential a headless firing holds: full `auth/drive` scope, not read-only | secrets.md states the full auth/drive scope the routine key mints. |
| AUD-0094 | MAJOR | OPEN | `PENDING_REVIEW` entities have no resolution path in code | not addressed in this pass. |
| AUD-0095 | MAJOR | OPEN | Files the routine writes back to Drive carry internal-audience content and are outside every redaction check | not addressed in this pass. |
| AUD-0096 | MAJOR | OPEN | Caps and the +/-0.8 uncertainty cap decorate; nothing binds a served number | not addressed in this pass. |
| AUD-0097 | MAJOR | OPEN | Peer medians are stored producer assertions on the serving tier, never reconciled with peer_scores; at subcap grain the column has no writer at all | not addressed in this pass. |
| AUD-0098 | MAJOR | OPEN | The three 'surviving mutants' in staged.py are killed by a test the mutation pair does not run | not addressed in this pass. |
| AUD-0099 | MAJOR | OPEN | verify_deployed.py --quick reports a pass when it could read nothing, and deploy.sh cannot fail on the verdict | not addressed in this pass. |
| AUD-0100 | MAJOR | OPEN | Nothing marks a DMA finished, and no line in the repo notices | not addressed in this pass. |
| AUD-0101 | MAJOR | FIXED | The never-cat rule is unenforced although the enforcement slot exists and is already used — one violation costs 116k-592k tokens | deny_bulk_read.py denies a whole-file read of the four named paths and names the bounded command instead. |
| AUD-0102 | MAJOR | OPEN | The batched challenge's ~150-250 tok/subcap budget is reachable only by emitting a verdict that does no work — and a ZERO-dimension verdict validates | not addressed in this pass. |
| AUD-0103 | MAJOR | OPEN | The G9 token gate is a ratchet re-fitted to each build, and the spec documents thresholds 29% below what the code enforces | not addressed in this pass. |
| AUD-0104 | MAJOR | OPEN | R25's funnel token math (+25-32% / +11% / +3%) has no code, test or dataset behind it, and the dud-sweep floor case is refuted arithmetically | not addressed in this pass. |
| AUD-0105 | MAJOR | DESIGNED_OUT | Nothing connects the research token budget to the reports' blocking word minimums, and no line of code anywhere measures a word count | report_spec carries the blocking word minimums and the 8-card floor; both refuse the render. |
| AUD-0106 | MAJOR | FIXED | None of the §4.3 optimisation apparatus exists in the live system; the installed dma-research is v2.3 with no knowledge graph | the installed dma-research now ships the engine; the token machinery described in the archive is reachable from the plugin. |
| AUD-0107 | MAJOR | MITIGATED | Thirteen workbook sheets named as INPUTS by the two templates are absent from the pinned contract-v3 workbook, leaving §3.2 and §3.3 with no source at all | a section whose declared inputs are all empty refuses and names them. |
| AUD-0108 | MAJOR | OPEN | The Client Profile's three source-of-truth declarations have no code path, and the five gates its H1 contract names do not exist | not addressed in this pass. |
| AUD-0109 | MAJOR | OPEN | The template's own 57-of-138 and 53 figures cannot be re-measured against the live corpus, because the live corpus has zero served pages | not addressed in this pass. |
| AUD-0110 | MAJOR | OPEN | Neither report degrades a peer figure, so app and report can print different medians for the same client with no reconciliation | not addressed in this pass. |
| AUD-0111 | MAJOR | OPEN | AG-04 enforces the named-peer sourcing burden on 1 of 12 contract sections that name a peer | not addressed in this pass. |
| AUD-0112 | MAJOR | OPEN | AG-04 does not enforce two of §6.5's four rules: dropped unknowns and the omitted share | not addressed in this pass. |
| AUD-0113 | MAJOR | OPEN | source_cell is contractually mandatory for every workbook peer figure and is enforced nowhere | not addressed in this pass. |
| AUD-0114 | MAJOR | OPEN | AG-04 does not cover every payload location that names a peer | not addressed in this pass. |
| AUD-0115 | MAJOR | OPEN | An unscanned estate is over-recommended by up to 28 fit points, because incumbent_covers has no 'we never looked' state | not addressed in this pass. |
| AUD-0116 | MAJOR | OPEN | An omitted readiness reads GREEN, defeating the engine's own stated amber default and the multiplicative safety property | not addressed in this pass. |
| AUD-0117 | MAJOR | OPEN | alignment_quote is worth 0.20 of the composite and is documented as 'never gated' | not addressed in this pass. |
| AUD-0118 | MAJOR | OPEN | Client Profile §6.4's counter-evidence pass exists in no code, while §6.2 from the same section did land — so this is an omission, not a boundary | not addressed in this pass. |
| AUD-0119 | MAJOR | OPEN | KPI baseline and readiness backing cells are contract-required, DB-typed, frontend-rendered — and validated by nothing | not addressed in this pass. |
| AUD-0120 | MAJOR | OPEN | No alert policy or notification channel exists for a stuck or quarantined ingest | not addressed in this pass. |
| AUD-0121 | MAJOR | FIXED | The gate registry holds 69, the routing table's own gate reference documents 32 of them | 1-gates.md carries a generated census of all 69 registry ids; --check fails a stale build. |
| AUD-0122 | MAJOR | OPEN | explain_gate's threshold history is structurally present and empty everywhere measured | not addressed in this pass. |
| AUD-0123 | MAJOR | OPEN | Ten production refusals sat unrepaired for four days while the Routine that must read them fired twice | not addressed in this pass. |
| AUD-0124 | MAJOR | OPEN | Gate M is the only gate with a test that CI never runs — and it exists because a client shipped with 85% unURLed evidence | not addressed in this pass. |
| AUD-0125 | MAJOR | FIXED | identifiers.find_fabricated() was built to reject client-supplied evidence ids and is called nowhere in production | find_fabricated is wired to prose citations, outside the keyed guard. |
| AUD-0126 | MAJOR | OPEN | The suite baseline is off by one and the prompt's single-venv instrument cannot run the suites at all | not addressed in this pass. |
| AUD-0127 | MAJOR | OPEN | One of the twelve skips is conditioned on the current data shape, hiding an untested behaviour | not addressed in this pass. |
| AUD-0128 | MAJOR | OPEN | Three mutation survivors in the chunked read-back path are reported as 'ok' | not addressed in this pass. |
| AUD-0129 | MAJOR | OPEN | No gate predicate has a mutation pair, and the readiness-silence path has zero test coverage | not addressed in this pass. |
| AUD-0130 | MAJOR | OPEN | Seven of twelve architecture gates run in CI with no test file, so a silently-broken gate reads as a pass | not addressed in this pass. |
| AUD-0131 | MAJOR | FIXED | One generated-artefact freshness check across thirty CI steps | three generated-artefact freshness checks in CI where there was one. |
| AUD-0154 | MAJOR | DESIGNED_OUT | The technographic scan is a side effect of category researchers with no owning agent | agents/research/technographic-scanner.md owns it: four layers worked deliberately, status earned by evidence kind, connector gaps emitted as search_requests rather than fabricated. Its JSON is asserted against the app's own parse_technographic_scan. |
| AUD-0156 | MAJOR | FIXED | No routine starts a DMA assessment: five Routines watch, rectify, refresh and promote, and none begins a run | dma-assessment-intake (trig_018eeMRDobRQXPZ4aobjfUih, 59 * * * *) prepares the binding for unstarted client folders and STOPS at the AskUserQuestion, which a headless firing cannot answer. Half-hourly was specified and the API enforces an hourly minimum; the canon records the built cadence, not the wished-for one. |
| AUD-0157 | MAJOR | DESIGNED_OUT | Template adherence was unverifiable: the template's identifier appeared nowhere in the plugin | engine/template.py diffs a local copy against contract.SHEETS — shape only, since the template's rows are placeholders by design. The CONTRACT is authoritative; the check exists to make divergence visible while somebody can still decide which side is wrong. |
| AUD-0162 | MAJOR | FIXED | No section's internal structure was specified anywhere, and no map said which app surface a section feeds | Section gains `blocks` (the ordered subheadings a body must carry, refused when missing or out of order) and `surfaces` (the payload sections it feeds). reports.render promotes each block to a real Heading2, which is the grain the app parses and embed.py scopes on — the pillar blocks carry the parenthesised (P1)..(P4) token the regex already looks for. gen_report_agents renders all of it, and --check fails the build on drift. |
| AUD-0166 | MAJOR | FIXED | The app reads three tabs the research contract does not define: Recommendations, Pillar_Summary, Category_Detail | Filed NOT_THIS_ENGINE on 2026-08-30 and OVERRULED by the owner the same day: the three tabs belong to the assessment stage, and the stage is now a recorded fact rather than an inference, so they can be required where they apply and forbidden where they do not. The reasoning that filed it stands and is why the fix took a stage key: adding them to contract.SHEETS unconditionally would have given every research workbook three tabs it is forbidden to fill, which is the same defect facing the other way. AUD-0173 is the fix. |
| AUD-0167 | MAJOR | MITIGATED | The live watchdog prompt named a revive path that resolves only under the plugin tree, and no verification scope was running the test that catches it | Two defects, one fix and one deliberate non-fix. The prompt said --revive dispatches 'through scripts/agent_run.py', which a routine standing at the repo root cannot open; the canon now names the qualified path. It was NOT re-pushed to the live trigger: the sentence is prose, the mechanism does not read it (watchdog._agent_run resolves from the installed plugin's own location), and re-pushing means replacing 14,561 characters on an hourly routine that currently SUCCEEDS with no way to read the stored prompt back and confirm it landed. The divergence is recorded in the canon's own §2d heading so it cannot pass as byte-for-byte agreement. The SECOND defect is why it survived: this test lives in scripts/tests/, which PR #15's verification scope (tests/ + plugins/dma-insights/scripts/tests/) did not include — it failed identically at the merge base. Verification now runs tests/, plugins/dma-insights/scripts/tests/, scripts/tests/ and the four app suites. |
| AUD-0174 | MAJOR | MITIGATED | Two of six live Routines were unhealthy and nothing watched Routine health | The watchdog watches RUNS; nothing watched ROUTINES. routine_health.py reads a list_triggers response and reports a verdict with the next move. NEITHER cause is a code defect this repo can fix: the FAILED one hit an individual spend limit (five_hour rate limit, rejected — raised at claude.ai/settings/usage), and the ABANDONED one is BLOCKED on a connector permission prompt that the plugin's own hook ALLOWS — verified by running the hook against that exact tool name — which means the hook did not run: a stale install, and a session cannot heal it because hooks bind once at session start. Both are the owner's to clear; the checker is what makes them visible next time. |
| AUD-0175 | MAJOR | OPEN | No owner-names-the-client channel exists at all | The owner's report ('routines have clients hardcoded') describes a defect already repaired in the live prompts — every live Routine chooses dynamically and the only pinned one was deleted 2026-08-20 — but the gap BEHIND the report is real and different: the owner can stop a Routine and can edit a stored prompt, and cannot say 'do this client next' through any interface that exists. docs/CLIENT-SELECTION.md specifies the channel against the interfaces that do exist — three Slack commands, a request file in the intake tree beside the client folders, one `--requests` flag on run_gate.py pick, and a threaded reply — and marks every section LIVE or NOT BUILT. Building it is the owner's call on scope. |
| AUD-0177 | MAJOR | FIXED | Nothing reported whether the system as a whole was ready, and a lane nobody measured read as a lane that passed | Every green report this project has been wrong about was green because something never looked — the doctor counting the checkout's files on a stale container, the classified profile nothing read, two unhealthy Routines nobody was told about. So readiness.py reports THREE verdicts, not two: READY, BLOCKED and NOT_MEASURABLE_HERE, and the third is never counted as ready. It re-derives nothing — every lane shells out to the check that owns it — and a container with no credential downgrades the connector lane to unmeasured rather than reporting a serving deployment as broken. The standing items no script can close (spend limit, stale-install provisioning, the unbuilt owner-names-the-client channel, connector grants) travel with the answer, each naming its owner and the document that specifies it. `--strict` exits non-zero on an unmeasured lane, which is what CI should use. Two defects in the first draft of the checker itself, both found by running it: the detail line took the LAST THREE lines of each check's output, so a fifteen-row doctor report came back described by whichever rows sorted last; and the credential downgrade was keyed on that whole output, so a STALE INSTALL — a row the install lane already owns — was reported as 'no live credential path from this container', a check inventing a reason for its own verdict. The gist is now the FAILING rows, and the downgrade requires every failing row to be about obtaining a credential. Lanes also declare a SCOPE (repository / container / external) so `goal_status.py` can fail the standing goal on a checkout defect without failing it on a stale install in whatever container happened to run the check — read from readiness.py rather than re-derived, so two readers of one answer cannot disagree about it. |
| AUD-0178 | MAJOR | FIXED | The findings ledger had drifted out of the vocabulary its own tool reads, and every mode of that tool had been dying on it | Found by running `aud_ledger.py --md`, which nothing does on a schedule: KeyError: 'BLOCKING' out of the summary, --open, --verify and --md alike, with .qa/AUD-DISPOSITIONS.md stale behind it for as long as the drift lasted. The third drift is the serious one — a finding citing a check name with no command would have been reported by --verify as proved while running nothing, which is the acceptance-ledger shape AUD-0050 caught. Severities normalised to the one vocabulary, the three commands written down, AUD-0166 reconciled to FIXED (its NOT_THIS_ENGINE call was overruled by the owner and AUD-0173 is the fix), AUD-0169 closed against AUD-0171 which had been sitting FIXED beside it. `load()` now refuses an unreadable ledger with a message naming the rows instead of a KeyError naming a dictionary, and a test pins the vocabulary, the citations and the currency of the generated markdown. |
| AUD-0132 | MINOR | FIXED | 17 of 47 agent manifests hardcode an absolute /home/user/Accelerate path instead of ${CLAUDE_PLUGIN_ROOT} | 32 manifests: 90 plugin paths -> ${CLAUDE_PLUGIN_ROOT}, 36 repo paths -> repo-relative. |
| AUD-0133 | MINOR | DESIGNED_OUT | fcntl id-minting is per-host, but the real exposure is that $RUN cannot be shared at all | evidence ids are allocated from the workbook's own register, so there is no host-local counter to share. |
| AUD-0134 | MINOR | OPEN | The behavioural-drift loop is populated and read, but closes only through a human-merged PR | not addressed in this pass. |
| AUD-0135 | MINOR | MITIGATED | DQ_Bank holds eight facets per subcap, not five — the template's own prose and its chain-integrity check disagree with its data | the DQ_Bank contract carries a Facet column and the facet vocabulary is one tuple. |
| AUD-0136 | MINOR | MITIGATED | Assessment Report §6.5 names no source at all for its peer deployment rows | peer rows are not produced by this engine; the gap is stated. |
| AUD-0137 | MINOR | OPEN | G7 tests the first word, not open-endedness, and runs only at build time | not addressed in this pass. |
| AUD-0138 | MINOR | DESIGNED_OUT | build_handoff.py hardcodes safeguard_gates, tech_utilization, critical_unknowns and org_capability_proxies to empty | the four facets are read from the workbook; one that never ran is NOT_RUN with a reason, never []. |
| AUD-0139 | MINOR | OPEN | followup.py's counter-evidence branch is dead: the code tests direction=='counter', the schema allows 'challenges' | not addressed in this pass. |
| AUD-0140 | MINOR | OPEN | `scripts/ingestion_status.py` cannot run without gcloud, and gcloud is absent from every fired routine container | not addressed in this pass. |
| AUD-0141 | MINOR | OPEN | The rejection ledger is wired but has no unattended consumer | not addressed in this pass. |
| AUD-0142 | MINOR | OPEN | Grain reconciles payload-against-served at 0.05, but never grain-against-grain | not addressed in this pass. |
| AUD-0143 | MINOR | FIXED | The work card exceeds its stated <=~750 tok in 790 of 851 cases under the invocation the protocol prescribes, and nothing gates card size | the card measures itself against a ceiling and STATES an overrun rather than hiding it. |
| AUD-0144 | MINOR | FIXED | offering_map's maturity_lift uses an M1-M5 scale that the charter forbids, and the same column is defined in the live database | the band/score distinction is stated and enforced; offering_map's own maturity_lift column is DB data and is listed as OPEN below. |
| AUD-0145 | MINOR | DESIGNED_OUT | The archive renderer's insight-card floor is 3 where the template's blocking minimum is 8 | report_spec carries the blocking word minimums and the 8-card floor; both refuse the render. |
| AUD-0146 | MINOR | OPEN | ET-09's peer allow-list is fed by the same empty table, so legitimate peers who are also corpus clients read as foreign-entity contamination | not addressed in this pass. |
| AUD-0147 | MINOR | OPEN | platform.recommendations' contract never declares r_layer, yet AG-01 blocks on it | not addressed in this pass. |
| AUD-0148 | MINOR | OPEN | The recommendation and roadmap dependency chains are ungated; only the platform chain reaches the sequencer | not addressed in this pass. |
| AUD-0149 | INFO | OPEN | No size ceiling exists anywhere, and the only real harness limit is 14 characters away from being hit | not addressed in this pass. |
| AUD-0150 | INFO | OPEN | Working tree carries an unrestored mutation from a sibling audit agent | not addressed in this pass. |
| AUD-0151 | INFO | OPEN | The greenfield and incumbent terms read the STAGING submissions table while claiming to read the promoted register | not addressed in this pass. |
