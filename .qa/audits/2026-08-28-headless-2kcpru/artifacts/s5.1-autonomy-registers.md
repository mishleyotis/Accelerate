# §5.1 — Autonomy readiness: the two registers

Every row is a **measured** item from this audit. Nothing here is inferred.

## A · PRESENT–HUMAN-DEPENDENT — every place a person currently acts

| # | The step | The human | Measurement |
|---|---|---|---|
| 1 | **Strip columns L–AG from four pillar sheets before handoff** | a researcher, by hand | `strip_working_area.py` is named by the workbook as the remedy and exists in neither tree (`grep -rl strip_working_area` over repo and `/tmp/dmar` = 0). `validate_workbook.py:25` reads `range(1,12)`, so an unstripped workbook passes its own validator and is refused one stage later |
| 2 | **Resume a run whose container exited** | whoever re-runs `orient.py --run $RUN` | `$RUN` is container-local; nothing persists it and no Routine drives the research stage |
| 3 | **Notice a HARD HALT** | whoever reads the transcript | `kg_reader guard` exits non-zero on a checksum mismatch; unattended, a hard halt with no listener is a silent stall |
| 4 | **Answer the `open_conflict` client discovery question** | a client contact | The documented exit requires a tie-breaker *plus* a logged discovery question; there is no addressee in a headless run |
| 5 | **Read the four watchdogs' output** | an operator | `synthesis_watchdog`, `backlog_sweep` → `goal_status.py`, whose only consumer is its own test. 0 hits in `ci.yml`; named in 0 of 3 live Routine prompts |
| 6 | **Decide the evidence mode (PUBLIC / HYBRID / INTERNAL)** | an analyst | 0 occurrences in the Backend Schema, the TRD, the DB (113 tables), or `contracts_data.json` |
| 7 | **Merge the Drive ledger snapshots into the repo** | the weekly rectification PR, reviewed by a person | ROUTINES.md: "the weekly rectification (the ONE routine that opens PRs) merges the snapshots"; its last run FAILED after 12 s |
| 8 | **Generalise a reported defect into a gate** | the owner reading a rendered page | 4 of 4 prose gates are instance detectors; 13 of 13 same-class paraphrases pass |
| 9 | **Regenerate `MCP-TOOLS.md`** | whoever remembers | 1 freshness check across 30 CI steps; the artefact is 4 commits stale in its stamp |
| 10 | **Re-vet a package after a REFUSE** | a fresh top-level session a human starts | "A package-vetter REFUSE is overturned only by a fresh top-level re-vet" |

## B · SILENT FAILURES — ranked above loud ones, because unattended they are permanent

| # | The failure | Why it is silent | Measurement |
|---|---|---|---|
| 1 | **A wrong-but-perfect conclusion promotes** | every one of the 69 gates checks an internal relation; none checks whether the evidence answers the question | `.qa/artifacts/s6.3-wrong-but-perfect.md` — 8 legs, all pass |
| 2 | **`ledger.py stats` crashes, so the token budget is never checked** | the caller is a model reading a soft rule | `NameError: name '_stats' is not defined` at `scripts/engine/ledger.py:125`, every invocation |
| 3 | **Resume skips the interrupted subcap and reports clean** | the ledger has no record for a subcap that never closed | measured on a real mid-category interrupt |
| 4 | **A card with no `readiness` ships hot** | the gate feeds its own omission back to the engine, so they agree | `validation2.py:1482` `or "green"`; max fit 62.0 → 99.0; HOT at subtotal 80.0 |
| 5 | **A syndicated press release scores maximum corroboration** | domains are counted, origins are not | `register.py:95` `count(DISTINCT e.source_domain)`; 3 domains ⇒ 5 of 5 |
| 6 | **The routing chain's second hop 404s** | the CI ceiling is pinned at the defect count | 50 of 55 rows → `rulebooks/<page>.md` which does not exist there; `MAX_BROKEN = 8`, broken = 8, exit 0 |
| 7 | **A dispatched in-process subagent starts unrouted** | SessionStart fires only for top-level sessions | 4 hook attachments in the parent, 0 across 3 of 3 subagent transcripts |
| 8 | **v8 report sections are stored under the wrong kind** | the parser keys on the section NUMBER | 9 of 11 mis-filed one slot early; §8 Recommendations stored as `gap_prioritization` |
| 9 | **A skeleton filled with empty prose closes a subcap** | the gates check structure, not substance | `orient.py --skeleton` output passes validation unmodified |
| 10 | **Coverage reports >100% and a negative gap count** | the denominator is a stale constant | `merge_evidence.py:185` `total_subcaps=836` against a true 851 ⇒ 101.8%, `no_evidence = −15` |
| 11 | **A vendor-named query is fired for a vendor-neutral question** | G10 never scans `q.primary` | 103 vendor names in the shipped KG's primary queries |
| 12 | **`data_source` reports DRIVE_PARSE for a run that never touched Drive** | it is a string literal, not a column read | `apps/api/dma_api/main.py:259` |
| 13 | **An `evidence_mode` badge renders empty in production** | the field is unguarded and no service produces it | `pages-d1-overview.jsx:290`, unguarded where both siblings are ternary-guarded |
| 14 | **A reordered lock sequence passes its own stability test** | both sides of the assertion derive from one dict | reorder mutation: test passed, 15/15 promote tests passed |

**The ordering that matters:** rows 1–5 of register B are the ones a human currently
catches by reading output. Deleting the human deletes the detector, not the defect.
