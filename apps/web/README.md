# web — Next.js frontend (App Router, SSR)

Renders the 38 surfaces across 7 dashboards from `svc_api` responses.
Tailwind tokens per `prototype/template.html`. The score→band→hex colour
resolver lives in exactly one module here — payloads carry no colour
(root `CLAUDE.md` invariants 6–7).

Built in the frontend stages of the Implementation Plan. Deployed as the
`web` Cloud Run service.
