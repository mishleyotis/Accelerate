"""Cross-entity pattern detection worker.

Clusters recurring sub-capability gaps + open issues ACROSS the entities of a
subvertical+catalogue cohort, so the D6 Health "Patterns" tab can surface
"this gap/issue recurs across N peers". Batch-only; mirrors `peer_patterns`.
"""
