Staging directory. deploy.sh copies packages/shared here before building the
worker image; the files are NOT committed, so packages/shared holds the single
copy of each contract and there is no second one to drift.
