# migrations — Alembic

Expand–migrate–contract; `CREATE INDEX CONCURRENTLY`; grants land in the
same revision as the table. The HNSW vector index (m=16,
ef_construction=64) is created once here, at migration time. Runs as the
`migrate` Cloud Run Job before every service roll.
