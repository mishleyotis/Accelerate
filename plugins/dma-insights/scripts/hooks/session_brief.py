#!/usr/bin/env python3
"""SessionStart hook — the thirty-word brief that saves a wrong first hour.

No network, no state: it prints the routing rule and the memory rule so a
fresh session neither re-produces a whole run to fix one card nor produces
anything before reading what past runs already learned.
"""
print(
    "dma-insights: route before you produce. One surface -> that page's "
    "surface producer, then finding-challenger, then page-consolidator; "
    "only the surface-producer submits or promotes. Read "
    "get_memory_digest before authoring anything, and end every "
    "production with the qa-overseer so the findings memory learns. "
    "Routing table: skills/dma-surface-production/05-lifecycle/routing.md"
)
