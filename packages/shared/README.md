# shared — cross-service contracts

Payload section contracts (from the Surface Specification — never invent a
field), vocabularies (band_t four values, stack statuses
CONFIRMED/INFERRED/CLAIMED/ABSENT, layer keys OPS/CUST/DATA/INFRA, evidence
ladder CURRENT…ARCHIVAL), redaction path markers, and the band boundary
fixture shared by the DB generated column test and the frontend resolver
test.
