# fixtures — the golden run

One complete six-page payload set derived from the Surface Specification
contracts (built at stage 1). Drives the invariant tests: band DB↔frontend
agreement, redaction walker snapshots (customer vs internal), grain
tolerance, writer-order stability, cross-page reconciliation
(composite↔pillar means, hero↔grid, O8≡C6, landscape↔register, roadmap ids
resolve), ETag/304, cursor stability under inserts.
